const list_directory = {
    name: 'list_directory',
    description: 'List the contents of a directory. Use recursive="true" to see the full tree structure.',
    usage: `
<tool name="list_directory" path="path/to/directory" recursive="true">
</tool>
    `
};

module.exports = list_directory;
