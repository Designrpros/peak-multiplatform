const CAPTURE_LIVE_VIEW_TOOL = {
    name: 'capture_live_view',
    description: 'Capture the current content of the Live View tab. Returns the HTML structure of the page being previewed.',
    usage: '<tool name="capture_live_view">\n</tool>',
    examples: [
        '<tool name="capture_live_view">\n</tool>'
    ]
};

module.exports = CAPTURE_LIVE_VIEW_TOOL;
